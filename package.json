{
  "name": "impulse-shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for Impulse applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "impulse",
    "shared",
    "types"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "yup": "^1.x"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "openai": "^4.87.3",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "yup": "^1.x"
  }
}
